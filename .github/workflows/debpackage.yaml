name: Build Debian package

on:
  push:
    branches:
      - debian
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:

      - name: Install Debian packaging dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y debhelper dpkg-dev devscripts equivs
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - name: Install build dependencies
        run: |
          sudo mk-build-deps -i -r --tool "apt-get -o Debug::pkgProblemResolver=yes --no-install-recommends --yes"
      - name: Create upstream tarball
        run: |
          VERSION=0.1.0
          tar -czf ../liballocs_${VERSION}.orig.tar.gz .
      - name: Build Debian package
        run: dpkg-buildpackage -rfakeroot -b -us -uc
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: debian-source-package
          path: |
            ../*.dsc
            ../*.debian.tar.*
            ../*.orig.tar.*
          retention-days: 7